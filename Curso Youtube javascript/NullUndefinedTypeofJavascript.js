var Numero = 0;

console.log(typeof Numero);// el typeof nos permite saber de que tipo es una variable que estamos declarando, de esta forma podemos imprimir el resultado

if(typeof Hola == "undefined"){//Si la variable no esta definida retorna undefined y si esta vacia retorna null
    console.log("La variable no esta definida")
}
else{
    console.log("La variable si esta definida")
}